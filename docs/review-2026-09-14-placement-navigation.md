# CASE移動・対応画像配置のレビュー（2026-09-14）

対象: `594129d` 時点の直近変更（CASE移動、New/Old開始行共有、参照画像リサイズ）。

## 修正した指摘

- P1: 対応画像の開始行がCASE最初の配置行の場合、開始セルだけが空いていれば、その下のセル・画像に重なっても計画が成功していた。画像全体と画像間の必要余白を検査し、衝突時は変更前に解析を停止する。途中行に揃える場合も同じ検査を適用する。
- P2: 隣接シートの解析・フォーカス失敗を記録しても、元シートへの復帰が成功すると「これ以上CASEはありません」で原因を隠していた。復帰後も対象シート名と元の失敗理由を表示する。

## 検証

- 修正前に追加した重なり・余白不足の回帰テスト2件が失敗することを確認。
- 修正後、ExcelIntegration以外の135件が成功。
- 実Excelテスト2件成功（計48秒）:
  - 一時ブックで双方向シート移動と、解析できない隣接シートの名前・理由の表示を検証。既存のブック識別・行操作・画像操作検証も通過。
  - Newを2枚先行配置し、異なる高さのOldを追加。参照画像の拡大と行追加の後も、各対応画像のStartRowとTopPointsが一致。2組目の配置画像・追加行を戻した後、参照画像リサイズをUndo/Redoし、期待する状態に一致。
- `git diff --check` 成功。

本レビューは上記変更と関連経路を対象とする。全機能の手動受入テストや、画像配置履歴全体のUI操作によるUndo/Redoは実施していない。重なり時に既存コンテンツを自動再配置する機能は含めず、安全停止する既存方針を維持する。
